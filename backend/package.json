{
  "name": "bones-cms-backend",
  "version": "1.0.0",
  "description": "Simple CMS backend",
  "main": "index.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build:frontend": "cd ../frontend && npm install && npm run build",
    "copy:frontend": "rm -rf public && mkdir public && cp -R ../frontend/build/* public/",
    "render-build": "npm run build:frontend && npm run copy:frontend"
  },
  "author": "Matthew Sanner",
  "license": "ISC",
  "dependencies": {
    "connect-mongo": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "mongoose": "^7.4.2",
    "nodemailer": "^6.9.4",
    "passport": "^0.6.0",
    "passport-local": "^1.0.0",
    "passport-local-mongoose": "^8.0.0"
  }
}
